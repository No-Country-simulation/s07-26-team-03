package com.physaflow.server.domain.service.calculation;

/*
 * =====================================================================
 * TEMPORARILY DISABLED — referencias a la entidad eliminada `Calculation`.
 *
 * El contrato del motor analítico se restaurará cuando se migre al
 * nuevo modelo: Assessment (input) + AssessmentResult (output) +
 * LayerAnalysis (desglose por capa). Ver rama feat/create-entities.
 * =====================================================================
 */
interface CalculatorServicePlaceholder {}
