package com.physaflow.server.application.mapper;

/*
 * =====================================================================
 * TEMPORARILY DISABLED — MapStruct referencia la entidad eliminada
 * `Calculation`. Se restaurará cuando el motor de cálculo exponga
 * un Assessment + AssessmentResult al endpoint /api/v1/calculator/estimate.
 * =====================================================================
 */
interface CalculatorMapperPlaceholder {}
