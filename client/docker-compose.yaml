name: physaflow-client

services:
  client:
    image: schweigenderflugel/physaflow
    container_name: physaflow-client
    hostname: localhost
    env_file:
      - ./.env
    ports:
      - 5173:80