name: Backend

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'
  pull_request:
    branches: ['*']
    paths:
      - 'server/**'

jobs:
  server-testing:
    runs-on: ubuntu-22.04

    #services:
    #  postgres:
    #    image: postgres:15-alpine3.20
    #    env:
    #      POSTGRES_USER: 'physaflow-test'
    #      POSTGRES_PASSWORD: '1cf4eb8f04a84637'
    #      POSTGRES_DB: 'physaflow-test-db'
    #    ports:
    #      - 5432:5432
    #    options: >- 
    #      --health-cmd pg_isready 
    #      --health-interval 10s 
    #      --health-timeout 5s 
    #      --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with: 
          fetch-depth: 0

